import React from "react";
import { Switch, Route, Redirect } from "react-router-dom";

const Routing = () => {
    return (
        <div>
            Routes
        </div>
    )
};

export default Routing;