import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        width: 300,
        flexGrow: 1,
        marginLeft: "500px"
    }
});

export default useStyles;